[pytest]
# 本仓库的 canonical 测试命令(此前没有):
#   "D:/Desktop/.venv-tools/Scripts/python.exe" -m pytest -q
#   加 --run-network 才跑联网用例; 覆盖率: --cov=rock_gd --cov-report=term-missing
testpaths = tests
addopts = -q -ra
filterwarnings =
    ignore::DeprecationWarning
